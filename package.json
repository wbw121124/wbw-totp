{
  "name": "wbw-totp",
  "version": "1.0.2",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "fabu": "pnpm run build && pnpm run check && pnpm publish --access public --loglevel verbose",
    "build": "pnpm tsup",
    "check": "pnpm pack --dry-run",
    "push": "git add . && git commit -m \"update\" && git push",
    "pp": "pnpm run push && pnpm run fabu",
    "gendocs": "pnpm typedoc",
    "gendocs:clean": "rimraf docs && pnpm typedoc",
    "gendocs:serve": "pnpm gendocs:clean && pnpm run serve:docs",
    "serve:docs": "pnpm serve docs"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.33.2",
  "type": "module",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "rimraf": "^6.1.3",
    "serve": "^14.2.6",
    "terser": "^5.46.2",
    "tsup": "^8.5.1",
    "typedoc": "^0.28.19",
    "typescript": "^6.0.3"
  },
  "homepage": "https://github.com/wbw121124/wbw-totp/",
  "dependencies": {
    "wbw-totp": "link:"
  }
}